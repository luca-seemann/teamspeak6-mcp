# Brings up a TeamSpeak 6 server with both query interfaces enabled, plus this MCP server
# alongside it. Intended for development and for the integration test suite.
#
#   docker compose -f docker/docker-compose.yml up -d
#
# The TeamSpeak server prints a generated serveradmin password on first start unless you set
# TSSERVER_QUERY_ADMIN_PASSWORD, which this file does.

services:
  teamspeak:
    image: teamspeaksystems/teamspeak6-server:latest
    container_name: teamspeak6
    environment:
      TSSERVER_LICENSE_ACCEPTED: accept
      # Query interfaces are off by default; the MCP server needs them.
      TSSERVER_QUERY_SSH_ENABLED: "1"
      TSSERVER_QUERY_SSH_PORT: "10022"
      TSSERVER_QUERY_HTTP_ENABLED: "1"
      TSSERVER_QUERY_HTTP_PORT: "10080"
      # Development credentials only. Never use these on a reachable server.
      TSSERVER_QUERY_ADMIN_PASSWORD: ${TSSERVER_QUERY_ADMIN_PASSWORD:-devpassword}
    ports:
      - "9987:9987/udp"   # voice
      - "30033:30033"     # file transfer
      # From 6.0.0-beta13 both query ports answer without credentials, as the ServerQuery guest:
      # SSH takes the user "guest" with any password, and the WebQuery takes a request with no
      # x-api-key header. Whatever the "Guest Server Query" group grants is granted to anyone who
      # reaches these ports, so bind them to 127.0.0.1 when the host is not private.
      - "10022:10022"     # SSH query
      - "10080:10080"     # HTTP WebQuery
    volumes:
      - teamspeak-data:/var/tsserver/
    restart: unless-stopped

  teamspeak6-mcp:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    container_name: teamspeak6-mcp
    depends_on:
      - teamspeak
    environment:
      # Settings bind from TeamSpeak:Profiles:<name>:<setting>; see "Every setting" in
      # docs/setup.md. A password enables the SSH interface, which is preferred when present.
      TSMCP_TeamSpeak__Profiles__default__Host: teamspeak
      TSMCP_TeamSpeak__Profiles__default__SshPort: "10022"
      TSMCP_TeamSpeak__Profiles__default__Username: serveradmin
      TSMCP_TeamSpeak__Profiles__default__Password: ${TSSERVER_QUERY_ADMIN_PASSWORD:-devpassword}
      # The WebQuery needs an API key minted over SSH with apikeyadd, so it cannot be preset here:
      # TSMCP_TeamSpeak__Profiles__default__WebQueryUrl: http://teamspeak:10080
      # TSMCP_TeamSpeak__Profiles__default__ApiKey: <key>
      # Tools stay read-only unless this is raised to Write or Destructive.
      TSMCP_TeamSpeak__Safety: ReadOnly
      # Inside the container the server listens on all interfaces, so it refuses to start without a
      # bearer token. Clients send it as "Authorization: Bearer <token>".
      TSMCP_TeamSpeak__Http__BearerToken: ${TSMCP_HTTP_TOKEN:?Set TSMCP_HTTP_TOKEN to a random value of at least 32 characters}
      # The SSH host key of each server is remembered on first connection and checked afterwards.
      # Keep the file on a volume, or a recreated container trusts whatever key it meets first.
      TSMCP_TeamSpeak__KnownHostsFile: /state/known_hosts
      # File tools pass content inline unless given a directory for local files; mount a volume
      # there. The bytes travel over port 30033 of the teamspeak service.
      # TSMCP_TeamSpeak__FileTransfer__LocalDirectory: /data/files
    ports:
      # Published on the loopback address only; publish more widely only behind your own network controls.
      - "127.0.0.1:7801:7801"
    volumes:
      - teamspeak6-mcp-state:/state
    restart: unless-stopped

volumes:
  teamspeak-data:
  teamspeak6-mcp-state:
